import styled from "styled-components";
import { getTheme } from "@utils/utils";

export const Styledbutton = styled.div`


.responsive-content table {
  width: 100%;                /* Makes the table take full width of the container */
  border-collapse: collapse;  /* Collapses borders to remove gaps */
}

.responsive-content td, .responsive-content th {
  padding: 8px;               /* Adds padding within cells */
  word-wrap: break-word;      /* Ensures word wrapping in table cells */
  text-align: left;           /* Aligns text to the left */
}
`;